access_key =  "{{ cloud.access_key }}"
secret_key = "{{ cloud.secret_key }}"
region = "{{ cloud.region }}"

vpc_name = "{{ cloud.vpc_name }}"
vswitch_name = "{{ cloud.vswitch_name }}"
security_group_name = "{{ cloud.security_group_name }}"
instance_name = "{{ cloud.instance_name }}"
instance_type = "{{ cloud.instance_type }}"

key_name = "{{ ssh.key_name }}"
public_key = "{{ ssh.public_key }}"

